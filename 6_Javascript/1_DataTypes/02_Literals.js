// Literals : literals are used to make code write easily and read comfortably 

var name = `hitesh's`
var name1 = "Hitesh\'s" // here i have to use the black slash for postoff S but in literal not need of this
let name2 = `${name} Choudhary`// here we add variable easily without any concatination 
console.log(name)
console.log(name1)
console.log(name2) 
console.log(name1.charAt(1));

/* ********************************************************* */

let num = 2;
let num1 = 3;
let result = num + num1;

console.log(`the addtion of ${num} and ${num1} is ${result}`);