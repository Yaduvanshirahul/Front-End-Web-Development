export const multi = (a,b)=> a*b;
export const sum = (a,b)=>{
    return a+b;
}

export const sub = (a,b)=> a-b;
export default sub; // here we export sub by default so no need to import it in curly braces and only one can be default in whole code

export  const Student_count = 30;